[project]
name = "app"
version = "0.1.0"
description = ""
authors = [
    {name = "Misati Nyambane",email = "misatiquanta@gmail.com"}
]
requires-python = ">=3.12"
dependencies = [
    "django (>=6.0.7,<7.0.0)",
    "djangorestframework (>=3.17.1,<4.0.0)",
    "transformers (>=5.14.1,<6.0.0)",
    "torch (>=2.13.0,<3.0.0)",
    "peft (>=0.20.0,<0.21.0)",
    "huggingface-hub (>=1.26.0,<2.0.0)",
    "gunicorn (>=26.0.0,<27.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
integration = [
    "django-migration-checker (>=0.8.0,<0.9.0)",
    "ruff (>=0.16.1,<0.17.0)"
]
